pub struct ConversationService;

impl Default for ConversationService {
    fn default() -> Self {
        Self::new()
    }
}

impl ConversationService {
    pub fn new() -> Self {
        Self
    }
}
